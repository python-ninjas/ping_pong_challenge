from django.shortcuts import render, HttpResponse, redirect

# Create your views here.
def index(request):
    return render('login_and_register/index.html')

def login(request):
    if request.methods != 'POST':
        return redirect('./')
    errors = []
    email = request.POST['email']
    pw_hash
    return redirect('./')
